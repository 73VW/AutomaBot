language: python
python:
  - "3.6"

install:
  - pip install -e .[fast,qa]

cache:
    pip
# command to run tests
script:
  - ./test.sh
# notifications
notifications:
  email:
    on_failure: never # default: always
    on_pull_requests: never
